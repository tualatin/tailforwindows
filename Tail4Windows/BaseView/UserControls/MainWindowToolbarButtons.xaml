<UserControl
  x:Class="Org.Vs.TailForWin.BaseView.UserControls.MainWindowToolbarButtons"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="24"
  d:DesignWidth="75"
  >
  <StackPanel Orientation="Horizontal">
    <Button
      Width="24"
      Command="{StaticResource MinimizeCommand}"
      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"
      DockPanel.Dock="Right"
      Style="{StaticResource MinimizeButton}"
      />
    <Button
      Width="24"
      Command="{StaticResource MaximizeCommand}"
      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"
      DockPanel.Dock="Right"
      Style="{StaticResource MaximizeButton}"
      />
    <Button
      Width="24"
      Command="{StaticResource CloseCommand}"
      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"
      DockPanel.Dock="Right"
      Style="{StaticResource CloseButton}"
      />
  </StackPanel>
</UserControl>
